
% 1 column
% no colsucc(X1, X2)

% 5 rows
rowsucc(1, 2).
rowsucc(2, 3).
rowsucc(3, 4).
rowsucc(4, 5).

% containers (c1, c2, c3)
container(c1, 1, 2). container(c1, 1, 3).
container(c2, 1, 4).
container(c3, 1, 5).

% colsum
colsum(1, 3).

% rowsums
rowsum(1, 0).
rowsum(2, 1).
rowsum(3, 1).
rowsum(4, 1).
rowsum(5, 0).

% expected result
% filled(1, 2).
% filled(1, 3).
% filled(1, 4).
