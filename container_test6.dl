
% 3 columns
colsucc(1, 2).
colsucc(2, 3).

% 3 rows
rowsucc(1, 2).
rowsucc(2, 3).

% containers (c1, c2, c3)
container(c1, 1, 1). container(c1, 1, 2). container(c1, 1, 3).
container(c2, 2, 2). container(c2, 3, 2).
container(c3, 3, 1).

% colsums
colsum(1, 0).
colsum(2, 0).
colsum(3, 0).

% rowsums
rowsum(1, 1).
rowsum(2, 0).
rowsum(3, 0).

% expected results

% no results (even better)
